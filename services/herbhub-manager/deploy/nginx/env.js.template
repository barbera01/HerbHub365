window.__HERBHUB_MANAGER_CONFIG__ = {
  auth: {
    disabled: ${SPA_AUTH_DISABLED},
    authority: "${SPA_AUTH_AUTHORITY}",
    clientId: "${SPA_AUTH_CLIENT_ID}",
    redirectUri: "${SPA_AUTH_REDIRECT_URI}",
    postLogoutRedirectUri: "${SPA_AUTH_POST_LOGOUT_REDIRECT_URI}",
    apiScope: "${SPA_AUTH_API_SCOPE}",
    apiAudience: "${SPA_AUTH_API_AUDIENCE}",
    requiredRole: "${SPA_AUTH_REQUIRED_ROLE}"
  }
};
